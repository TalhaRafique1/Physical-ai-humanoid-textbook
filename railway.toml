# Railway configuration for textbook app

[build]
builder = "docker"
dockerfilePath = "Dockerfile.railway"

[variables]
PORT = 3000
DATABASE_URL = "postgresql+asyncpg://textbook_user:textbook_password@db:5432/textbook_generation"
DEBUG = true